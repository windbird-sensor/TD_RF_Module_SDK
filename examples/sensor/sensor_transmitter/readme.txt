Sensor simple Transmitter template for new TDxxxx RF module projects.

This example project use EFM32 CMSIS, the emlib peripheral library, the
libttdcore utility library and the libtdtools library to monitor a button press
duration using a polling technique and send a corresponding message over the
UART.


